# Compartmental models

This folder implements three versions of the LSHTM model -- the original model in R, an exact translation of the R model into Python, and a translation of the Python model into Starsim.

See `tests/test_compartmental.py` for related tests.